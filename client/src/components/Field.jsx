import { Typography } from '@mui/material'
import React from 'react'

const Field = ({field}) => {
  return (
    <>
          <Typography variant="h4">Field : {field}</Typography>
    </>
  )
}

export default Field